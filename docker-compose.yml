version: "3.0"

services:
  avahi:
    build: ./avahi
    network_mode: host

  mqtt:
    image: eclipse-mosquitto
    command: ["/usr/sbin/mosquitto", "-v", "-c", "/mosquitto/config/mosquitto.conf"]
    ports:
      - "1883:1883"
